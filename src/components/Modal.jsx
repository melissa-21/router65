import React from 'react';
import { Modal } from '@mui/material';

const ModalL = ({active, setActive}) => {
    return (
        <div className="modal">
            <div className=".modal_content">

            </div>

        </div>
    );
};

export default Modal;